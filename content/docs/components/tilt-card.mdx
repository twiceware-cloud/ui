---
slug: components-tilt-card
title: Tilt Card
description: Interactive 3D tilt card that responds to cursor, adds depth and motion.
---

<DemoCodePreview
    path="demo/components/tilt-card/default.tsx"
/>

## Installation

<Tabs defaultValue="cli" className="mt-3">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli" className="mt-4">
        <InstallationCommand command={`npx shadcn@latest add "~website/r/gsap/tilt-card"`} />
    </TabsContent>
    <TabsContent value="manual" className="mt-4">
        <Steps>
            <Step>
                Install the following dependencies
            </Step>
            <InstallationCommand command="npm install gsap @gsap/react" />
            <Step>
                Copy and paste the following code into your project
            </Step>
            <CodePreview path='components/gsap/tilt-card.tsx' collapsible></CodePreview>

            <Step>
                Update the import paths to fit your project setup
            </Step>
        </Steps>
    </TabsContent>
</Tabs>

## Examples

### Custom Spotlight

Set `hightlightClass` to customize the highlight shadow, however, you like.

<DemoCodePreview
    path="demo/components/tilt-card/custom-spotlight.tsx"
/>

### High Tilt

Set `maxTilt`  to create a more dramatic tilt effect.

<DemoCodePreview
    path="demo/components/tilt-card/high-tilt.tsx"
/>


## Props

### TiltCard

| Prop           | Type      | Default |
|----------------|-----------|---------|
| maxTilt        | number    | 10      |
| highlightClass | string    | -       |
| className      | string    | -       |
| children       | ReactNode | -       |
