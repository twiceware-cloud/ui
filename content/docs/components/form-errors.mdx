---
slug: components-form-errors
title: Form errors
description: A playful animation where characters bounce in sequence across the text.
---

<DemoCodePreview
    path="demo/components/bouncing-text/default.tsx"
    v0Link="/gsap/bouncing-text"
/>

## Installation

<Tabs defaultValue="cli" className="mt-3">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli" className="mt-4">
        <InstallationCommand command={`npx shadcn@latest add "~website/r/gsap/bouncing-text"`} />
    </TabsContent>
    <TabsContent value="manual" className="mt-4">
        <Steps>
            <Step>
                Install the following dependencies
            </Step>
            <InstallationCommand command="npm install gsap @gsap/react" />
            <Step>
                Copy and paste the following code into your project
            </Step>
            <CodePreview path='components/gsap/bouncing-text.tsx' collapsible></CodePreview>

            <Step>
                Update the import paths to fit your project setup
            </Step>
        </Steps>
    </TabsContent>
</Tabs>

## Props

### BouncingText

| Prop      | Type              | Default |
|-----------|-------------------|---------|
| repeat    | boolean \| number | true    |

<span className="text-muted-foreground">Inspired by Martyn Knapton's work</span>
