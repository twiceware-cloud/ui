---
slug: components-spring-button
title: Spring Button
description: A button that shrinks on press and springs back with bounce.
---

<DemoCodePreview
    path="demo/components/spring-button/default.tsx"
    v0Link="/gsap/spring-button"
/>

## Installation

<Tabs defaultValue="cli" className="mt-3">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli" className="mt-4">
        <InstallationCommand command={`npx shadcn@latest add "~website/r/gsap/spring-button"`} />
    </TabsContent>
    <TabsContent value="manual" className="mt-4">
        <Steps>
            <Step>
                Install the following dependencies
            </Step>
            <InstallationCommand command="npm install gsap @gsap/react" />
            <Step>
                Copy and paste the following code into your project
            </Step>
            <CodePreview path='components/gsap/spring-button.tsx' collapsible></CodePreview>

            <Step>
                Update the import paths to fit your project setup
            </Step>
        </Steps>
    </TabsContent>
</Tabs>

## Examples

### Deeper Press

Set the `scale` value between 0 and 1 to lower values make the button shrink more when pressed.

<DemoCodePreview
    path="demo/components/spring-button/deeper-press.tsx"
    v0Link="/gsap/spring-button"
/>


## Props

### SpringButton

| Prop    | Type    | Default |
|---------|---------|---------|
| scale   | number  | 0.85    |
| shaking | boolean | true    |
