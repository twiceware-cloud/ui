---
slug: components-stagger-on-scroll
title: Stagger on Scroll
description: Stagger elements into view with scroll-triggered fade, slide, zoom, or blur effects.
links:
    api: https://gsap.com/docs/v3/Plugins/ScrollTrigger
---

<DemoCodePreview
    path="demo/components/stagger-on-scroll/default.tsx"
    v0Link="/gsap/stagger-on-scroll"
    props={[
        {
            title: "Effect",
            name: "effect",
            type: "select",
            defaultValue: "scale",
            values: ["scale", "slideInRight", "blur", "random"],
        },
    ]}
/>

## Installation

<Tabs defaultValue="cli" className="mt-3">
    <TabsList>
        <TabsTrigger value="cli">CLI</TabsTrigger>
        <TabsTrigger value="manual">Manual</TabsTrigger>
    </TabsList>
    <TabsContent value="cli" className="mt-4">
        <InstallationCommand command={`npx shadcn@latest add "~website/r/gsap/stagger-on-scroll"`} />
    </TabsContent>
    <TabsContent value="manual" className="mt-4">
        <Steps>
            <Step>
                Install the following dependencies
            </Step>
            <InstallationCommand command="npm install gsap @gsap/react" />
            <Step>
                Copy and paste the following code into your project
            </Step>
            <CodePreview path='components/gsap/stagger-on-scroll.tsx' collapsible></CodePreview>

            <Step>
                Update the import paths to fit your project setup
            </Step>
        </Steps>
    </TabsContent>
</Tabs>

## Props

### StaggerOnScroll

| Prop     | Type                                            | Default |
|----------|-------------------------------------------------|---------|
| effect   | `scale` \| `slideInRight` \| `blur` \| `random` | `scale` |
