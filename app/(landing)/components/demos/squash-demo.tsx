'use client'

import { SquashText } from '@/components/gsap/squash-text'

export const SquashDemo = () => {
  return (
    <SquashText className="font-semibold text-5xl text-secondary tracking-tight">
      Push the Pixels
    </SquashText>
  )
}
